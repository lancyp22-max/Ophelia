version: "0.1"
name: "Sealed Content Manifest"
design_status: specified
enforcement_status: partially_implemented
enforcement_scope: "The public-shell builder follows a curated split, but this manifest is not yet mechanically authoritative over every publication path."
enforcement_evidence:
  - scripts/build_public_shell.sh
  - scripts/public_shell_audit.sh
intent: "Classify repository surface by exposure tier for stewardship routing."

tiers:
  public_safe:
    description: "Safe for routine public publishing."
    paths:
      - "README.md"
      - "docs/**"
      - "policies/**"
      - "index.html"
      - "script.js"
      - ".github/workflows/**"

  translated_private:
    description: "May be shared only in translated/minimized form."
    paths:
      - "backups/**"
      - "library/**"

  steward_only:
    description: "Requires steward review before any external distribution."
    paths:
      - "tools/**"
      - "scripts/**"
      - "src/**"

  core_sealed:
    description: "Protected core; requires full protected-change protocol."
    paths:
      - "policies/protected-change-protocol.v0.1.yaml"
      - "policies/public-exposure-guardrails.v0.1.yaml"
